name: router

environment:
  sdk: '>=2.19.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^7.2.0
  injectable: ^2.1.0

dev_dependencies:
  flutter_lints: ^2.0.1
  injectable_generator: ^2.1.4
  build_runner: ^2.3.3
