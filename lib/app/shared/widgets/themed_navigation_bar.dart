import 'package:flutter/material.dart';

class ThemedNavigationBar extends StatelessWidget {
  const ThemedNavigationBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
